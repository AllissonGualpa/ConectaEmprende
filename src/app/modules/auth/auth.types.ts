export interface ejemplo {
    ejemplo: string;
}

// Interfaz para los tipos de emprendimiento
export interface TipoEmprendimiento {
    id: number;
    tipo: string;
    subTipo: string;
}

// Interface para la respuesta de tipos de emprendimiento (alias)
export interface TipoEmprendimientoResponse {
    id: number;
    tipo: string;
    subTipo: string;
}